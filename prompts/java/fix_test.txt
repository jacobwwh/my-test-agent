You are an expert Java developer specializing in unit testing. You previously generated a JUnit 5 test for the method below, but it had issues. Fix the test based on the feedback provided.

## Target Method

Class: `{{ target.class_name }}`
Method: `{{ target.method_name }}`

```java
{{ target.method_signature }}
```

## Full Class Source

```java
{{ target.class_source }}
```

{% if dependencies %}
## Dependencies

{% for dep in dependencies %}
### {{ dep.kind }}: `{{ dep.qualified_name }}`

```java
{{ dep.source }}
```

{% endfor %}
{% endif %}

## Previous Test (Iteration {{ previous_test.iteration }})

```java
{{ previous_test.test_code }}
```

## Feedback

{% if not test_result.compiled %}
### Compilation Errors

The test failed to compile with the following errors:

```
{{ test_result.compile_errors }}
```

Fix all compilation errors so the test compiles successfully.
{% elif not test_result.passed %}
### Test Failures

The test compiled but some tests failed:

```
{{ test_result.test_output }}
```

{% if test_result.failed_tests %}
Failed test methods: {{ test_result.failed_tests | join(', ') }}
{% endif %}

Fix the failing tests. Make sure assertions match the actual behavior of the method under test.
{% endif %}
{% if test_result.coverage %}
### Coverage Gaps

Current coverage:
- Line coverage: {{ "%.1f" | format(test_result.coverage.line_coverage * 100) }}%
- Branch coverage: {{ "%.1f" | format(test_result.coverage.branch_coverage * 100) }}%

{% if test_result.coverage.uncovered_lines %}
Uncovered lines: {{ test_result.coverage.uncovered_lines | join(', ') }}
{% endif %}
{% if test_result.coverage.uncovered_branches %}
Uncovered branches:
{% for branch in test_result.coverage.uncovered_branches %}
- {{ branch }}
{% endfor %}
{% endif %}

Add test cases to cover the uncovered lines and branches.
{% endif %}

## Requirements

1. Keep the refinement only for the target method.
2. Keep the fixed test class in the same package as the target class.
3. Do not add tests for other methods.
4. Output the COMPLETE fixed test class, not just the changed parts.
5. Keep the test class name exactly `{{ target.class_name.split('.')[-1] }}Test`.
6. Ensure the test compiles and all assertions are correct.
7. Maintain or improve test coverage for the target method only.
8. Output ONLY the complete Java test class source code, wrapped in a single ```java code block.
