You are an expert Java developer specializing in unit testing. Your task is to generate comprehensive JUnit 5 test cases for the given method.

## Target Method

Class: `{{ target.class_name }}`
Method: `{{ target.method_name }}`

```java
{{ target.method_signature }}
```

## Full Class Source

```java
{{ target.class_source }}
```

{% if dependencies %}
## Dependencies

{% for dep in dependencies %}
### {{ dep.kind }}: `{{ dep.qualified_name }}`

```java
{{ dep.source }}
```

{% endfor %}
{% endif %}
{% if imports %}
## Import Statements

```java
{% for imp in imports %}
{{ imp }}
{% endfor %}
```
{% endif %}
{% if existing_test_summary %}
## Existing Test File Summary

File: `{{ existing_test_summary.file_path }}`

### Imports

```java
{% for imp in existing_test_summary.imports %}
{{ imp }}
{% endfor %}
```

### Class Signature

```java
{{ existing_test_summary.class_signature }}
```

### Class-level Fields

{% for field in existing_test_summary.field_declarations %}
- `{{ field }}`
{% endfor %}

### Helper Method Signatures

{% for helper in existing_test_summary.helper_method_signatures %}
- `{{ helper }}`
{% endfor %}

### Existing Test Method Signatures

{% for test_method in existing_test_summary.test_method_signatures %}
- `{{ test_method }}`
{% endfor %}

Use this summary to avoid duplicate imports, fields, helper methods, and test method names. Reuse compatible shared objects and helpers when they already exist.
{% endif %}

## Requirements

1. Put the test class in the same package as the target class.
2. Generate tests only for the target method.
3. Do not generate tests for other methods.
4. Generate a complete JUnit 5 test class that compiles and runs independently.
5. Name the test class exactly `{{ target.class_name.split('.')[-1] }}Test` (e.g. if the class is `Calculator`, name it `CalculatorTest`).
6. Use `@Test` annotations from `org.junit.jupiter.api`.
7. Cover normal cases, edge cases, and boundary conditions for the target method only.
8. Use descriptive test method names that explain the scenario being tested.
9. Include necessary imports.
10. If the method depends on external services or complex objects, use Mockito for mocking.
11. When `## Existing Test File Summary` is present, avoid duplicate imports, fields, helpers, and test method names.
12. Output ONLY the complete Java test class source code, wrapped in a single ```java code block.
